/**
 * 只所以不在代码中直接引用，是因为 eslint 会报 no-undef 的错误，因此用如下方式处理
 */
/* eslint-disable */
export const host = 'https://api.yiquanxinhe.com/graphql'
/* eslint-enable */


// home
export const API_TUNNELBATCHLIST = `${host}`
